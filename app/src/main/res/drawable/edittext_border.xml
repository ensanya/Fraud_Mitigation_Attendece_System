<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent"/>
    <stroke
        android:width="2dp"
        android:color="@color/editTextBorderColor"/> <!-- Change the color as per your preference -->
    <corners android:radius="8dp"/> <!-- Change the radius as per your preference -->
</shape>
